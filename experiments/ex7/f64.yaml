# Experiment 7, rung 3 of 4 -- filters=64.
#
# Inherits everything from config.yaml and changes ONLY the filter counts.
# See f16.yaml for why both conv stages share one value.

extends: config.yaml

convolution:
  conv1_out: 64
  conv2_out: 64
