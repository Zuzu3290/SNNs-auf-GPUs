# Kernel Execution Records — 03 June 2026

Platform: Google Colab T4 GPU  
Branch: Operators  
Extension: `snn_forward` built via `src/learning/setup.py build_ext --inplace`  
Input shape: `[B=4, N=512, T=25]` — batch × neurons × timesteps

---

## Standard Forward Pass

| Metric | Value |
|--------|-------|
| Output shape | `(4, 512, 25)` |
| Spike rate | 0.0 % |
| NVML | Not available on this GPU tier |

```python
spikes = lif.forward(inp, voltage, v_th=1.0, tau_inv=0.1)
```

Spike rate of 0.0 % on random input with default threshold (1.0) is expected —
random uniform values rarely exceed the threshold in a single timestep without
accumulated membrane potential.

---

## Profiled Forward Pass

| Metric | Value |
|--------|-------|
| Kernel time | 0.018 ms |
| Spike rate | 0.2 % |
| Power / Energy | N/A — NVML not available on Colab T4 free tier |

```python
spikes, elapsed_ms, pwr_before, pwr_after, energy_mj, nvml_ok = \
    lif.forward_profiled(inp, voltage, v_th=1.0, tau_inv=0.1)
```

Timing via CUDA events is valid. Power and energy fields return 0 / False
because `-DSNN_HAS_NVML=0` was passed at build time — the profiler falls back
to timing-only mode, which is the correct behaviour on virtualised GPU
environments.

---

## Benchmark — Custom Kernel vs PyTorch Baseline

100 forward passes averaged. Input `[4, 512, 25]`.

| Path | Time per forward |
|------|-----------------|
| Custom CUDA kernel (`snn_forward`) | **1.455 ms** |
| PyTorch baseline (Python LIF loop over T timesteps) | 4.727 ms |
| **Speedup** | **3.25×** |

The PyTorch baseline iterates over T=25 timesteps in Python, calling
`v_new = v * (1 - tau_inv) + x` and `(v_new >= v_th).float()` per step.
The custom kernel fuses all timesteps into one GPU launch, eliminating the
Python loop overhead and reducing kernel launch count from 25 to 1.

---

## Training Run (Cell 8)

| Setting | Value |
|---------|-------|
| `kernel` | ON |
| `epochs` | 1 |
| `iterations_per_epoch` | 20 |
| Exit code | 1 (failed) |

The training process exited with code 1. The kernel itself loaded and ran
correctly (Cells 5–7 passed). The failure is in `src/learning/main.py` —
likely a missing dependency or dataset path on Colab, not related to the
CUDA extension. To be investigated separately.

---

## Notes

- The 3.25× speedup is measured on a T4 (sm_75, Turing architecture).
- The gap widens at larger batch sizes and neuron counts where GPU parallelism
  is better utilised.
- NVML energy readings require a bare-metal GPU (not virtualised); on local
  hardware with NVML available, remove `-DSNN_HAS_NVML=0` from `setup.py`
  to enable real power and energy measurements.


The current kernel has a fundamental issue that also limits performance: it launches one thread per (b, n, t) — but LIF has a temporal dependency where v(t) depends on v(t-1). With T threads all reading and writing voltage[b*N + n] simultaneously, the temporal sequence is a race condition. The speedup measured is real (fused launch vs Python loop) but the kernel is not computing the correct LIF dynamics under concurrent writes.

The researched approach that fixes this and gives the real performance edge is temporal register tiling — the same pattern used in efficient RNN/LSTM CUDA kernels:

Launch B×N threads (one per neuron, not per timestep)
Each thread owns one (b, n) neuron and loops over T in order
Voltage lives in a register for the entire T loop — zero global memory traffic for the state
Spikes written out per step, inputs streamed in sequentially
On top of that, warp ballot (__ballot_sync) gives a free warp-wide spike mask per step — enabling sparse downstream propagation (only send spikes where neurons actually fired).

The two together match what SpikingJelly's cupy backend and Norse's CUDA kernels do internally.

Concretely:


Current:   B×N×T threads, voltage in global mem, race condition
Fixed:     B×N   threads, voltage in register, T loop in-thread
Addition:  __ballot_sync → spike mask per warp per timestep
Expected gain over the current 3.25× result: significantly higher correctness + another 2-4× throughput from eliminating the T global memory round-trips.


lif_temporal.cu — the corrected kernel. Template <bool WriteMask> gives two instantiations with zero runtime branch cost. Key mechanics:

__ballot_sync(0xffffffff, active) first — all warp threads agree on who is in-bounds before diverging
Voltage v lives in a register for the full T loop — no global memory traffic for state
__ballot_sync(active_mask, fired) each step — 32-neuron spike bitmap, lane 0 writes it to spike_mask[warp, t]
snn_binding.cpp — adds temporal_forward and temporal_forward_ballot to Python alongside the existing forward / forward_profiled.

engine.cu — three dispatch routes now:

Config	Kernel	Use
accelerate: false	lif_basic fixed 256	Benchmark reference
accelerate: true, temporal: false	lif_basic + GPU attributes	Legacy accelerated
accelerate: true, temporal: true	lif_temporal + GPU attributes	Correct path
setup.py — lif_temporal.cu added to sources. Push and re-run Cell 4, then test with:


spikes = lif.temporal_forward(inp, voltage)
spikes, mask = lif.temporal_forward_ballot(inp, voltage)
print("Spike mask shape:", mask.shape)  # [ceil(B*N/32), T]